# AI Workflow Note

- **Tools**: ChatGPT / Claude for generating boilerplate (Express routes, SQL schema).
- **Sped up**: Writing the TipTap integration and file-upload middleware.
- **Rejected/Changed**: AI suggested MongoDB – rejected to keep setup simple. Also adjusted the TXT→JSON conversion to preserve paragraph breaks.
- **Verification**: Manually tested all endpoints with Postman and validated the editor’s autosave behavior.
